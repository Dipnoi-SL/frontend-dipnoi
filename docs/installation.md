# Installation

1.  Ensure you are using the correct node/npm versions (install `nvm` first):

        nvm install

2.  Install all dependencies:

        npm install

3.  Start the server:

        npm run start:dev
